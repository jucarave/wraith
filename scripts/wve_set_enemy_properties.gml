/*
 * wve_set_enemy_properties(enemy, spriteIndex, hp, spd, walkFrames, money)
 */
 
var enemy = argument[0];
 
enemy.sprite_index = argument[1];
enemy.hp = argument[2];
enemy.mHp = argument[2];
enemy.spd = argument[3];
enemy.walkMInd = argument[4];
enemy.money = argument[5];
